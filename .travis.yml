language: cpp
sudo: false
cache: apt

dist: focal
addons:
    apt:
        packages:
        - verilator
        - libsystemc-dev



branches:
    only:
        - master
        - dev_travis

jobs:
    include:
        - stage: sw_build
          name: build software
          script: make

